# 단계별 개선 실행

분석 기준: `SERVICE-AUDIT-2026-09-08.md`. 기존 원형 편집과 로그인 없는 사용을 유지한다.

## 이번 구현의 검증 기준

| 단계 | 구현 | 승인 기준 |
|---|---|---|
| 1. 신뢰 | 실제 저장 결과 표시, 재시도·백업, 무료/Pro 안내와 가격 상태 | 저장 실패 시 Saved 표시 없음; 최신 상태 재시도; 가격 실패 시 결제 시작 차단 |
| 2. 첫 사용 | 자동 꾸미기 대신 비차단 첫 일정 안내, 직접 보기 선택, 접근성 | 6초 뒤 자동 마법사 없음; 일정 추가/템플릿 접근; 보기 직접 선택; 경계 키보드 조절 |
| 3. 로딩 | 내보내기·폰트의 필요 시 로딩 | 초기 JS 감소 확인; 첫 PNG/PDF·공유·위젯·단일 파일 내보내기 유지 |
| 4. 유입·계측 | 템플릿 CTA와 유효 행동 추적, 민감한 일정 이름 제거 | 고정 식별자 사용; 외부 공유/다운로드 구분; 허용 목록에 등록된 UTM 보존 |

전체 단위 테스트·타입 검사·린트·웹/단일 HTML 빌드와 관련 브라우저 시나리오를 실행한다. 작성자와 다른 검토자가 변경을 확인한다.

## 2026-09-08 구현 결과

- 1~4단계의 위 표 구현 완료. 제목/시각 편집은 `meaningful_schedule_edit`로 기존 `schedule_edit`과 구분하며 프리셋·스타일·Undo/Redo는 제외한다.
- 실제 DAYS·캐시 저장 상태, 실패 시 재시도 및 기존 가져오기로 복원 가능한 JSON 백업을 제공한다.
- 모바일 헤더는 두 줄로 분리하고 5개 보기를 직접 선택한다. 원형 조절기의 방향키/Home/End와 실제 시간 범위를 제공한다.
- 가격 조회 실패·대기 중에는 결제를 시작하지 않는다. 8개 언어의 무료/Pro 범위를 정리했다.
- 메인 JS gzip은 약 5,569.84KB → 381.31KB로 약 93% 감소했다. 이는 메인 JS 크기 비교이며 전체 페이지 로딩 시간의 감소율은 아니다. 내보내기 폰트 약 5.18MB gzip은 필요한 시점에 로드하므로 큰 청크 경고는 남는다.
- 정적 콘텐츠 68개 HTML에 중복 없이 계측 로더가 주입됐다. 운영 호스트에서만 작동하며 문서의 허용된 UTM만 전달한다. 기존 앱 GA 초기화 전체를 대체한 것은 아니다.
- 검증: 전체 74개 파일·634개 테스트, 린트·타입 검사, 웹/단일 HTML 빌드, 기존 E2E 14개 묶음 통과. 별도 신규 UX 브라우저 검사 25개 통과(320/390/1440px, 첫 안내·접근성 구조·보기·저장 오류/백업/재시도).
- 독립 리뷰에서 장시간 경계 이동과 초기 복원 경합을 발견해 회귀 테스트와 함께 수정했다. 각 구현 영역 독립 검토 승인 완료.

검증 스크린샷과 로그: `.omc/research/service-audit/` (로컬 분석 자료, Git 제외).

## 출시 후 관찰

첫 일정 편집과 실제 저장 성공 → D1/D7 의미 있는 재사용 → Pro 진입/가격 표시/결제 시작 순서로 본다. 기존 `schedule_edit`은 어떤 undo 이력이든 포함하므로 새 유효 편집 이벤트와 혼합하지 않는다.

두 상황별 콘텐츠는 학습 계획과 직장인 시간 균형이다. 기존 템플릿에서 시작하며 광고 집행·외부 게시·가격 변경은 이번 코드 변경에 포함하지 않는다. 유입이 적을 때는 통계적 우월성을 주장하지 않고 5명 안팎의 과제 관찰로 막히는 지점을 먼저 찾는다.

결제 완료의 서버 집계, 실제 고객 D7·유료 유지 결과, 새 탐색의 사용자 조사 및 추가 레이아웃 실험은 출시 후 데이터가 필요한 단계다. 코드 구현 완료와 사업 효과 확인을 구분한다.
